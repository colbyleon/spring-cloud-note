server:
  port: 8761

spring:
  application:
    name: eureka-server

  security:
    user:
      name: root
      password: root

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

